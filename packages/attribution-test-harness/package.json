{
  "name": "attribution-test-harness",
  "version": "1.0.0",
  "description": "Docker sandbox for testing all AIACTA Proposals 1–4 against a simulated publisher ecosystem (§12.1)",
  "author": "Eric Michel",
  "scripts": {
    "up": "docker compose up --build",
    "down": "docker compose down",
    "test:e2e": "jest --testPathPattern=scenarios"
  },
  "devDependencies": { "jest": "^30.3.0", "axios": "^1.6.0" }
}
